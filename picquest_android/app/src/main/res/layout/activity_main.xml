<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ListView
        android:id="@+id/main_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/main_scan_button" />

    <Button
        android:id="@+id/main_scan_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/main_manual_ip"
        android:text="@string/main_search" />

    <EditText
        android:id="@id/main_manual_ip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/main_manual_connect"
        android:hint="ENTER_MANUALLY_IP"
        android:maxLength="15"
        android:padding="8dp"
        android:singleLine="true"
        android:text="192.168.0.1"
        android:textColor="@android:color/white"
        android:textColorHint="@android:color/darker_gray"
        android:textSize="18dp" />

    <Button
        android:id="@id/main_manual_connect"
        android:layout_width="120dp"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="CONNECT" />

</RelativeLayout>